import { User } from './types';
export const getUserFromApi = () => {
    //Get Api call here and return the user object;
  return null;
}

export const storeUserInBackend = (user: User) => {
  //push user object to Backend via API
}